"use client";
import React from "react";
import { useRouter } from "next/navigation";

const Page = ({ users }) => {
  const router = useRouter();
  return <div>Post: </div>;
};

export default Page;
